{
  "creator": {
    "email": "${TEST_USER_EMAIL}",
    "password": "${TEST_USER_PASSWORD}",
    "displayName": "${TEST_USER_DISPLAY_NAME}"
  },
  "donor": {
    "email": "${TEST_DONOR_EMAIL}",
    "password": "${TEST_DONOR_PASSWORD}",
    "displayName": "${TEST_DONOR_DISPLAY_NAME}"
  },
  "admin": {
    "email": "${TEST_ADMIN_EMAIL}",
    "password": "${TEST_ADMIN_PASSWORD}",
    "displayName": "${TEST_ADMIN_DISPLAY_NAME}"
  },
  "newUser": {
    "email": "${TEST_NEW_USER_EMAIL}",
    "password": "${TEST_NEW_USER_PASSWORD}",
    "displayName": "${TEST_NEW_USER_DISPLAY_NAME}"
  }
}
